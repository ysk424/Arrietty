# Arrietty 開発引き継ぎ

最終更新: 2026-08-26

現在の実機安定点: Version 0.5.0 — CYCPLUS T2と右VIVEコントローラで3 m幅のオーバルトラックを1周完走

現在の実装: Version 0.7.9 — P5/Crr 0.020をロードバイク初期値とするT2制御、5 km/h惰性停止、CSC実円盤停止判定、緑・オレンジ計器

## プロジェクトの最終目標

SteamVR HMDを装着し、Blender内の楽しい世界をサイクルトレーナーで走行・飛行できるGPL-3.0-or-laterのBlender Extensionを作る。ArriettyはVersion 0.8.0の空で機能完了とする。Google由来の世界はArrietty完了後に、別のBlenderプラグイン「Secret World」として開発する。

実装順序は機能を分離して進める。

1. BlenderのOpenXR VRセッションへ入る／終了する
2. HMD正面とテンキーで開始位置・方向を調整する
3. CYCPLUS T2の速度で前進する
4. 実機のハンドル操作で曲がる
5. 操舵の体感を実車と一致させる
6. ステム付近へ計器盤を表示する（Version 0.7.0）
7. 日時と緯度に基づく空を作る（Version 0.8.0）

GoogleタイルはArriettyのVR、走行、操舵から独立した後段プラグインである。Blenderでは1 Blender Unit = 1 m、地面はZ=0、目の高さはZ=1.5 mとする。

## 2026-08-24の実機合格点

- BlenderのOpenXRセッションをSteamVR HMDで開始し、初期Cubeを確認した。
- テンキー`4`/`6`で左右回転、`8`/`2`でHMD正面への前後移動ができた。
- CYCPLUS T2のFTMS通知から速度・ケイデンス・パワーを受信し、100 m直進を完走した。
- Manekkoで帽子用だったVIVE TrackerのIDを特定したが、専用受信機が必要なため操舵には使用しなかった。
- 右VIVEコントローラを自転車のステムへ固定し、左右の姿勢を安定して受信した。
- Version 0.5.0で幅3 m、中心線約143 mのオーバルトラックを1周完走した。
- 操舵感には調整余地があるが、「曲がれる」「周回できる」という当日の合格条件は達成した。初期結果は想定より良好だった。

## 利用環境

- Blender: 5.2.0 LTS
- Blender実行ファイル: `C:\Users\azoo\git\build_windows_Release_x64_vc17_Release\bin\blender.exe`
- OS: Windows x64
- CPU: AMD Ryzen 9 9950X3D
- GPU: GeForce RTX 5070 Ti
- メモリ: 64 GB
- HMD: HTC VIVE
- VRランタイム: SteamVR / OpenXR
- サイクルトレーナー: CYCPLUS T2
- 操舵: 右VIVEコントローラをステムへ固定
- 開発リポジトリ: `C:\Users\azoo\git\Arrietty`

## 機器IDと役割

| 機器 | SteamVR/OpenVR ID | 現在の役割 |
|---|---|---|
| 右VIVEコントローラ | `LHR-9EFF8645` | ステムに固定して操舵入力 |
| 左VIVEコントローラ | `LHR-0B253252` | Arriettyでは未使用 |
| Manekkoの帽子用Tracker | `LHR-CC5F5D2C` | 今回は不採用。専用受信機が必要 |

右コントローラの機種名はOpenVR上で`VIVE Controller Pro MV`。実機テストでは100回中100回の有効姿勢を受信した。中央から右は約-21.2°、中央から左は約+15.4°、右端から左端は約37.7°だった。

右コントローラはハンドル回転軸から約15–20 cm先のステムに固定されている。現在の操舵は位置ではなく相対回転を使うため、この距離の位置補正は不要である。

## 入力と出力の分担

- HMD表示: Blender OpenXR → SteamVR → HTC VIVE
- HMD正面: Blenderが保持するOpenXR viewer poseをテンキー8/2の位置調整時だけ読む
- 操舵: OpenVRから右コントローラ姿勢を走行中だけ60 Hzで読む。OpenVRクライアントはBlenderプロセス中に再利用する
- 速度・ケイデンス・パワー: CYCPLUS T2のBluetooth FTMS通知をBleakでプッシュ受信する
- 手動調整: テンキー
- IC-KP09: 将来の外部キー入力候補。現在は未実装
- Polar心拍計: 電源・受信候補の確認まで。現在は未実装

T2は通知受信なのでBluetooth値のポーリングをしない。コントローラ姿勢は連続量のため、走行中だけOpenVRで取得し、停止時には読み取りスレッドも終了する。

## 座標と方向

### Blender / HMD

- Blenderは右手系、Z-up、1 unit = 1 m。
- 地面はZ=0、XR base poseの高さは常にZ=1.5 m。
- HMD正面はOpenXR viewer poseのローカル`-Z`軸をBlender XY平面へ投影して使う。
- T2走行の初期方向は保存済みの`arrietty_heading`を使い、HMDの視線から切り離す。
- `arrietty_heading`はBlender XY規約に統一し、0°=+X、90°=+Y、±180°=−X、−90°=−Yとする。
- 走行中のXR base pose位置と角度だけを更新し、Z方向へは移動しない。

### 右コントローラ

- SteamVRはY-up。
- テンキー`0`を押した後、最初の有効姿勢をハンドル中央として記録する。
- 現在姿勢を`R_current`、中央姿勢を`R_center`とし、`R_current @ R_center.T`で取り付け角を除去する。
- SteamVR世界Y軸まわりの相対角を操舵角にする。
- 正が左、負が右。実機テストで符号を確認済み。

## Version 0.6.5の暫定操舵値

これらは「まず曲がる」ための初期値であり、次回の体感調整対象。

- 仮ホイールベース: 1.05 m
- ハンドル角から前輪操舵角へのゲイン: 0.50
- 中央デッドゾーン: 1.5°
- 適用操舵角の上限: ±15°
- コントローラ取得: 60 Hz
- 平滑化: 1サンプルごとに新値を25%反映
- Blender移動更新: 20 Hz
- 追跡タイムアウト: 0.5秒
- 移動モデル: `turn = distance / wheelbase * tan(steering_angle)`

コントローラ追跡を失うと移動を一時停止し、再受信すると再開する。

2026-08-25、1周143 mの自動終了直後にBlenderがクラッシュした。クラッシュログは`vrclient_x64.dll`内のアクセス違反を示しており、走行終了時の`openvr.shutdown()`とBlenderで継続中のOpenXRセッションが競合したと判断した。Version 0.6.2では走行終了時に姿勢取得スレッドだけを停止し、OpenVRクライアントはBlenderプロセス中に再利用する。

## 操作手順

1. SteamVR、HMD、右コントローラを起動する。
2. 右コントローラをステムへ固定し、ハンドルを中央にする。
3. Blenderで`test_data/arrietty_3m_track.blend`を開く。
4. Arriettyパネルの「Dive into Secret World」でVRを開始する。
5. テンキー`4`/`6`でパネルの「Start Direction」をコース方向へ合わせる。
6. T2を数回漕いで起こす。
7. ハンドルを中央に保ったままテンキー`0`を押す。Start Directionが初期進行方向、最初のコントローラ姿勢が操舵角0°になる。
8. T2と右コントローラの両方が受信されると開始音が鳴る。
9. 開始音の後に走行する。距離や周回数では終了せず、テンキー`0`をもう一度押しても停止しない。
10. 走行中にテンキー`7`を押すと飛行モード、もう一度押すと地上モードへ切り替わる。
11. Arriettyパネルの「Back to Real World」でVRから戻る。この時だけ通常の走行停止処理とCSVの終了処理を行う。

小型周回テストシーンは開始位置`(0, -10, 1.5)`、開始方向`+X`、1周約143 m。大型フライト世界は約3.2 km × 2.4 km、開始位置`(0, -320, 1.5)`、開始方向`+X`、1周約2.6 km。どちらも周回数に関係なく走り続ける。

## Version 0.6.5の飛行モード

- テンキー`0`で走行を開始した後、Blenderのキー識別子`NUMPAD_7`（テンキー7）で地上モードと飛行モードを切り替える。
- テンキー`7`は走行開始キーではなく、走行中に飛行／地上を何度でも切り替えるキー。
- T2のFTMS速度を使い、10 km/hまでは高度0 mとする。
- 10 km/hを超えた分について、1 km/hあたり1 m上昇する。
- 高度の式は`max(0, speed_kmh - 10) m`。15 km/hで5 m、20 km/hで10 m。
- XR base poseのZは、地上からの高度にHMD目線の1.5 mを加えた値。
- 地上へ切り替えたとき、走行を終了・中止したとき、エラーが起きたときは高度0 mへ即時に戻す。
- 飛行中も地上と同じ右コントローラ操舵を使う。
- 飛行状態は`flight.py`へ分離し、地上のXY走行計算は変更しない。

2026-08-25の実機ログでは、速度10.95→6.32 km/hに対して目標高度0.95→0.00 mが正しく計算されていた一方、HMDが地上へ戻らなかった。下降式や積分ではなく、開始地点用`base_pose_location`を実行中の連続高度にも使っていたことが原因と判断した。Version 0.6.5ではBlenderのライブXR移動用`XrSessionState.navigation_location.z`へ高度の絶対値を設定する。CSVには実高度、目標高度、XR base／navigation／viewerのZを分けて記録する。

同日のVersion 0.6.4実機試験では、テンキー`7`で飛行モードへ入り、最高25.86 km/h・目標高度15.86 mを記録した。大型フライト世界の空中にあるオレンジ色のリングを通過し、集落のビル間も飛行できた。離陸と空中操舵は合格、減速着陸はVersion 0.6.5で再確認する。

未実装の飛行挙動は、速度変化を滑らかに高度へ反映する処理、滑空、緩やかな着陸、最高高度、地形との衝突である。まず平面上のテストシーンと実機で直接的な速度・高度対応を確認してから追加する。

## Version 0.6.5の走行ログ

- ファイル名は常に`arrietty_ride.csv`。
- 保存先は現在の`.blend`と同じフォルダ。未保存シーンではBlenderの一時フォルダ。
- 新しい走行開始時に上書きし、日時別CSVを蓄積しない。
- 各T2通知で速度、ケイデンス、パワー、距離、周回数、飛行状態、実高度、目標高度、XRの各Z、XY位置、進行方向を追記してflushする。
- 「Back to Real World」で`BACK_TO_REAL_WORLD`行を書き、CSVを閉じる。
- BLEまたは操舵のエラー時はデータ保全のためエラー行を書いて閉じる。

## 保存済みテストデータと配布物

- `test_data/arrietty_3m_track.blend`: 幅3 m、約143 mのオーバルトラック
- `test_data/arrietty_straight_100m.blend`: 幅3 m、100 mの直線
- `test_data/arrietty_flight_world.blend`: 約3.2 km × 2.4 km、約2.6 km周回路の大型走行・飛行世界
- `arrietty-0.5.0.zip`: ローカルで生成したBlender Extensionパッケージ
- `arrietty-0.6.2.zip`: 4周走行、座標補正、飛行モード、OpenVRクラッシュ対策を含むパッケージ
- `arrietty-0.6.3.zip`: 無制限走行、固定名CSV、大型世界を含むパッケージ
- `arrietty-0.6.4.zip`: 飛行切り替えをテンキー7へ変更したパッケージ
- `arrietty-0.6.5.zip`: XRライブ高度を使った下降修正を含むパッケージ
- `arrietty-0.7.8.zip`: P7/Crr上限までの比較、Numpad +/-、5 km/h惰性停止を含むパッケージ
- `arrietty-0.7.9.zip`: P5/Crr 0.020を平地ロードバイク初期値とするパッケージ

ZIPのローカルパス:

`C:\Users\azoo\git\Arrietty\arrietty-0.7.9.zip`

## 次回の作業順序

1. `arrietty-0.7.9.zip`をインストールし、P5/Crr 0.020が接続時に自動適用されることを確認する。
2. ケイデンス0で5 km/hへ下がったとき、表示と前進が止まり、再度ペダルを回すと低速でも再発進できることを確認する。
3. 「Back to Real World」後、CSVの`t2_control_preset`が5、必要な区間の`low_speed_coast_stopped`が1であることを確認する。
4. 3ボタン装置の接続方式と入力コードを確認してから、ブレーキ／エレベーター上げ／エレベーター下げを実装する。
5. 飛行を人力飛行機モデルへ発展させ、速度依存の抗力、揚力、エレベーター入力から上昇下降を計算する。
6. Version 0.8.0で日時・緯度に基づく空を実装し、Arriettyを完了する。
7. その後、Google由来の世界を別プラグイン「Secret World」として設計する。

## 2026-08-26 Version 0.7.0実装

- HMD固定HUDではなく、通常のBlenderオブジェクトで構成した32 cm幅の3D計器盤を採用した。
- Blender 5.2の`XrSessionState.controller_grip_location_get(context, 1)`で右OpenXRグリップの描画世界座標を取得し、その約10 cm上へ置く。
- 生のコントローラー6DoF回転は文字の揺れになるため使わず、画面の向きは走行方向と既存の平滑化済み操舵角から作る。
- OpenXRグリップ位置がない時は、走行位置の前方68 cm、路面から約1.12 mの仮想ステムへフォールバックする。
- Nパネルから前後、左右、高さ、スケールを調整できる。計器盤はVR開始時に生成し、VR終了時に削除するため`.blend`へ恒久保存しない。
- 表示は速度、心拍数、時計、ケイデンス、パワー、距離、周回数、飛行高度、GROUND/FLIGHT、ローカルX/Y。Polar心拍受信はまだ未実装で、現在は`HR -- bpm`。
- `secret_world_ride_surface=True`のメッシュを路面として検出し、XR base poseのZを路面高+1.5 mにした。次の位置に走行面がない時は海上走行を避けるためXY移動を一時停止する。
- `test_data/Tuval-1.blend`の原点ではFunafuti滑走路走行面をZ=1.45996 mで検出し、地上の目線をZ=2.95996 mへ設定できることをBlender 5.2で確認した。
- 最初の配布ビルドv0.7.0は拡張登録中の制限データアクセスを検出した。登録フェーズからデータ掃除を外した修正版をv0.7.1とする。
- 実機OpenXRセッションでは標準コントローラーアクションがなく、グリップ座標がゼロだった。操舵用OpenVRからHMDローカルの右コントローラー相対位置も発行し、v0.7.2では走行開始後に実ステム位置を使えるようにした。
- v0.7.3では同じXY位置の走行面高を再利用し、停止中の計器更新が毎回メッシュへレイキャストしないようにした。
- v0.7.4ではNパネルのStart Pose Zにも走行面高を含め、Tuvalu原点で実際のXR base Zと同じ2.96 mを表示する。
- v0.7.4実走ログではケイデンスとパワーが0になった後も、T2のFTMS速度が17.03 km/hから3.28 km/hへ52.535秒かけて減衰し、その間に仮想世界を138.177 m進んだ。通信タイムアウトでは止められない現象である。
- T2のGATTを実機確認し、FTMS service 0x1826とは別にCSC service 0x1816、CSC Measurement 0x2A5Bを通知できることを確認した。Feature値`0x0003`でwheel/crank双方に対応し、累積wheel revolutionが実円盤とともに変化する。
- v0.7.5ではFTMS速度を表示・移動の基礎値として残しつつ、CSC wheel revolutionイベントの停止を物理停止ゲートとする。停止待ち時間は直前の1回転時間×1.5+0.25秒を0.75〜4.0秒へ制限する。CSCが利用できない時だけ従来のFTMS速度へフォールバックする。
- CSVは移動に使った`speed_kmh`と生の`ftms_speed_kmh`、CSC wheel revolution、CSC event time、停止判定を別列に記録する。
- 計器は黒い背景メッシュを廃止して発光文字だけにした。右コントローラー位置は最初の有効値を自転車ローカル座標へ一度だけ校正し、その後の追跡微振動を無視する。文字列とルート行列も値が変わった時だけ更新する。
- v0.7.5実走ログのCSC event intervalとFTMS速度を比較すると、推定ホイール周長の中央値は約2.09 mで両者はほぼ一致した。速度のデコード式ではなく、T2側のシミュレーション条件を明示する必要があると判断した。
- T2のFitness Machine Feature `824000000c600000`を読み、Control Point 0x2AD9がwrite/indicate、Indoor Bike Simulation ParametersとWheel Circumferenceの設定に対応することを実機確認した。
- Control PointへRequest Control `00`を送り応答`800001`、続いて平坦路設定`11000000002833`を送り応答`801101`を得た。設定値は風速0 m/s、勾配0%、Crr 0.0040、Cw 0.51 kg/m。
- v0.7.6ではこの2手順を接続ごとに行い、両方の成功応答後だけFTMS/CSC通知を開始する。拒否・タイムアウト時はERRORとして走行を開始しない。CSVに`t2_control_status`を追加した。
- v0.7.6の計器色は、速度・単位・走行データ・位置を緑、心拍・時計をオレンジとする。
- v0.7.6の実走では主な惰性区間が15.66 km/hから0まで49.002秒・121.136 m、13.48 km/hから0まで42.535秒・94.828 mだった。ユーザー評価は「高性能ロードレーサーならあり得る許容最大程度で、減速は遅い」。停止機能そのものはArriettyの基本機能として充分と判断した。
- 体感パラメータ探索のため、v0.7.7では勾配0%、風速0、Cw 0.51を固定し、CrrだけをP1=0.004、P2=0.008、P3=0.012、P4=0.016へ変える。キーはNumpad 1/3/5/9。
- 走行中のプリセット要求はmain threadからthread-safe queueへ入れ、BLE event loop上でControl Pointへ送る。成功応答後だけ`applied_control_preset`を更新する。計器へP番号を表示し、CSVの`t2_control_preset`へ各サンプルの適用値を保存する。
- T2実機へP1→P2→P3→P4→P1を連続送信し、全設定で成功応答`801101`を確認した。試験後はP1へ戻した。
- v0.7.7実走ではP4/Crr 0.016が4候補中で最良だった。P4の主な惰性区間は13.86 km/hから5.00 km/hまで35.002秒・89.314 m、その後の完全停止までさらに約10秒・10.302 mだった。
- v0.7.8では既存ログの意味を保つためP1〜P4を変更せず、P5=0.020、P6=0.024、P7=0.0255を追加する。P7はFTMSのCrr 1バイト値の上限。Numpad 9でP4を直接選び、Numpad +/-で上下へ移動する。
- v0.7.8ではケイデンス0かつT2生速度が0より大きく5 km/h以下なら表示・移動速度を0とする。低速ペダリング中は生速度を使い、CSC停止判定も独立して残す。CSVへ`low_speed_coast_stopped`を追加する。
- Metacycle 0.1.6を調査した。同プロジェクトは勾配と速度から経験式でFTMS Target Resistance Levelを毎秒送るが、T2では抵抗範囲1〜10へすぐ飽和するため定数は移植しない。負荷を調整可能にすること、実機上限を確認すること、トレーナー速度と仮想速度を分ける設計を知見として採用する。Crr上限でも不足した場合に限り、直接抵抗レベルを次の比較対象とする。
- v0.7.8実走ではP5/Crr 0.020を「普通のロードバイクの平地として自然」、P6/Crr 0.024を「普通の自転車の候補」と評価した。v0.7.9はP5を接続時の初期値とする。
- 将来の物理ブレーキ用にボタンを3個用意している。入力装置の電気・通信方式が確定するまで実装せず、暫定案はブレーキ／エレベーター機首上げ／エレベーター機首下げ。押している間だけ入力し、離すと中立へ戻す。
- 飛行モードは将来、人力飛行機として扱う。現在の速度だけで高度を決める式から、速度依存の空力抵抗、揚力、機体ピッチまたは上昇率をエレベーター入力で制御するモデルへ分離する。地上のCrrと飛行中の空力負荷を同じ定数で扱わない。

Secret World実装時は、API、利用規約、キャッシュ、座標参照系、原点移動、実寸スケールをArriettyの操舵系とは別モジュールとして扱う。

## ビルド番号

同じ機能バージョンで配布ZIPを再ビルドするときも、成果物を上書きせずパッチ番号を1つ上げる。Version 0.6系では`0.6.0`、`0.6.1`、`0.6.2`、`0.6.3`、`0.6.4`、`0.6.5`のように増やす。

## 現在は実装しないもの

- VIVE Trackerによる操舵
- コース中心線への自動追従
- IC-KP09入力
- Googleタイル取得・配置
- 地形勾配と連動する動的Bluetooth負荷制御（平坦路への初期化はv0.7.6で実装済み）

Polar心拍はVersion 0.7.0の計器盤で値を表示する対象だが、受信方法は実装時に決める。既存の周回成功状態を壊さず、まず操舵感と飛行モードの実機確認を完了してから次の機能へ進む。

## 2026-08-25終了時チェックポイント

- ユーザー評価は「新しい世界は良い」「充分楽しい」。Google由来タイルがなくても、空中リングをくぐり、ビル間を飛ぶだけで毎日楽しめるフィットネス体験になり得る。
- Version 0.6.4でテンキー`7`からの離陸、オレンジ色リング通過、ビル間飛行に成功した。飛行中の操舵もできている。
- 同試験では減速時に見た目の高度が地上へ戻らなかった。CSV上の速度・目標高度計算は正常だった。
- Version 0.6.5で高度の反映先をXRのライブ`navigation_location.z`へ変更し、下降を絶対高度で設定するよう修正した。Blenderテストとパッケージ検証は合格済みだが、HMDでの減速着陸は未確認。
- 現在の実機確認用ZIPは`C:\Users\azoo\git\Arrietty\arrietty-0.6.5.zip`。
- コードは`origin/main`へPUSH済み。次回は上記の着地確認から再開する。
- 今後もGoogle世界とは別に、リング、ゲート、ビル、山などを使ったArrietty単体の飛行フィットネスとしての楽しさを残す。
